# Requirements for verse content generation scripts
# Now using the unified verse-content-sdk

# Verse Content SDK - provides all tools for embeddings, audio, images, and deployment
# Install from GitHub:
git+https://github.com/sanatan-learnings/verse-content-sdk.git

# Note: The SDK includes all necessary dependencies:
# - openai (for DALL-E 3, embeddings, GPT)
# - elevenlabs (for text-to-speech)
# - requests (for downloading images)
# - Pillow (for image processing)
# - python-dotenv (for environment variables)
# - PyYAML (for theme configuration)
# - sentence-transformers (for local embeddings)
# - torch (for local models)
